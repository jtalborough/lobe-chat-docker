# ------------------------------------------------------------------
# LobeChat Environment Variables
# ------------------------------------------------------------------
# Copy this file to .env and fill in the values.
# In Portainer, you will add these variables in the UI instead of using a .env file.

# --- Required Variables --- #

# OpenAI API Key
OPENAI_API_KEY=

# Cloudflare Tunnel Token
CLOUDFLARE_TUNNEL_TOKEN=

# --- Database Configuration --- #

# A strong, unique password for the PostgreSQL database.
POSTGRES_PASSWORD=

# A long, random, and secret string for encrypting credentials. (e.g., openssl rand -base64 32)
KEY_VAULTS_SECRET=

# --- SSO & NextAuth Configuration --- #

# A long, random, and secret string for NextAuth. (e.g., openssl rand -hex 32)
NEXT_AUTH_SECRET=

# A comma-separated list of the SSO providers you want to enable (e.g., 'github', 'google').
NEXT_AUTH_SSO_PROVIDERS=github

# --- GitHub SSO Provider Example --- #
# (Only needed if 'github' is in NEXT_AUTH_SSO_PROVIDERS)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# --- Optional S3 Object Storage --- #
# If you are not using S3, leave these blank in the docker-compose.yml file.
# If you are using S3, provide the correct values here.
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=
S3_BUCKET=
S3_PUBLIC_DOMAIN=
